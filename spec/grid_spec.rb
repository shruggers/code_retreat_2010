require 'spec_helper'
require 'code_retreat'

describe Grid do
  
  let(:cells){
    [
      [Cell.new(false),Cell.new(false),Cell.new(false)],
      [Cell.new(false),Cell.new(false),Cell.new(false)],
      [Cell.new(false),Cell.new(false),Cell.new(false)]
    ]
    
  }
  
end